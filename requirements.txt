# ── Shared (local + deployed) ───────────────────────────────────────────────
pdfplumber==0.11.0
tiktoken>=0.6.0
chromadb>=0.5.6
requests>=2.31.0
rank_bm25>=0.2.2
fastapi>=0.110.0
uvicorn>=0.29.0
python-multipart>=0.0.9
streamlit>=1.30.0
python-dotenv>=1.0.0

# ── Embedding backend ────────────────────────────────────────────────────────
# sentence-transformers: used when EMBEDDING_BACKEND=hf_bge_small (deployed).
# Pulls in torch (CPU) and huggingface-hub. ~300 MB runtime RAM.
# Not needed if you run EMBEDDING_BACKEND=ollama locally with Ollama installed.
sentence-transformers>=3.0.0

# ── Local-only (Ollama client) ───────────────────────────────────────────────
# The ollama package is only needed if you run EMBEDDING_BACKEND=ollama locally.
# In the deployed Docker image EMBEDDING_BACKEND=hf_bge_small so this is unused
# but kept here so `pip install -r requirements.txt` still works locally.
ollama>=0.3.0
